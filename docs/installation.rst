============
Installation
============

At the command line::

    $ easy_install django_celery_async_view

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_celery_async_view
    $ pip install django_celery_async_view
